includes:
    - phpstan-baseline.neon

parameters:
    level: 8
    paths:
        - src
        - database
    tmpDir: build/phpstan
    ignoreErrors:
        # Searchable is a public consumer-facing trait (used in host apps, not in src).
        -
            identifier: trait.unused
            path: src/Concerns/Searchable.php
